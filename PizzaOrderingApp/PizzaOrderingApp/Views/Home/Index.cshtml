@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1>Welcome to the Pizza Ordering App</h1>
    <a href="@Url.Action("Register", "Account")">Register to Order Pizza</a>
</div>
